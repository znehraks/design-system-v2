{
  "name": "@designc/design-system-v2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:tokens": "pnpm --filter @designc/foundation build",
    "build:themes": "pnpm --filter @designc/theme build",
    "theme:build": "node scripts/build-theme-packs.mjs",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --passWithNoTests",
    "theme:validate": "node scripts/validate-theme-packs.mjs",
    "check:contrast": "node scripts/check-theme-contrast.mjs",
    "check:imports": "node scripts/check-import-boundaries.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^25.0.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.4",
    "vitest": "^4.0.6"
  }
}
